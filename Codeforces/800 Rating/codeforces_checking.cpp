/*بِسْمِ اللَّهِ الرَّحْمَنِ الرَّحِيم,*/
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t, i;

    cin >> t;
    char codeforces;

    for ( i = 0 ; i < t ; i++)
    {
        cin>>codeforces;

        if ( codeforces == 'c')
        {
            cout<<"YES"<<endl;
        }
        else if ( codeforces == 'o')
        {
            cout<<"YES"<<endl;
        }
        else if ( codeforces == 'd')
        {
            cout<<"YES"<<endl;
        }
        else if ( codeforces == 'e')
        {
            cout<<"YES"<<endl;
        }
        else if ( codeforces == 'f')
        {
            cout<<"YES"<<endl;
        }
        else if ( codeforces == 'r')
        {
            cout<<"YES"<<endl;
        }
        else if ( codeforces == 's')
        {
            cout<<"YES"<<endl;
        }
        else
        {
            cout<<"NO"<<endl;
        }

    }
}
